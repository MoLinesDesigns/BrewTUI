# 99. Delta vs baseline 2026-05-01

> Auditor: claude (orquestador) | Fecha: 2026-05-21 | Vs baseline: superaudit-report.2026-05-01/

## Cifras comparadas

| Metrica | 2026-05-01 | 2026-05-21 | Delta |
|---------|------------|------------|-------|
| Version producto | 0.6.1 | 1.2.1 | +6 minor (6 releases) |
| Archivos TypeScript | 141 | 178 | +37 |
| Lineas TypeScript | 16 596 | 19 951 | +3 355 |
| Archivos Swift | 22 | 27 | +5 |
| Lineas Swift | 3 837 | 4 918 | +1 081 |
| Total hallazgos (deduplicados) | 98 | 61 | **−37** |
| Criticos | 2 | 1 | **−1** |
| Altos | 22 | 14 | **−8** |
| Medios | 38 | 22 | **−16** |
| Bajos | 36 | 24 | **−12** |
| Cobertura test TS | 24,8% (35/141) | ~33% (60/178) | +8 puntos |
| Tests Swift en CI | No | Si (30 @Test methods) | OK |

**Lectura:** reduccion absoluta de 37 hallazgos pese a haber crecido el codigo +20% (TS) y +28% (Swift). Reduccion proporcional aun mayor.

---

## Hallazgos del baseline RESUELTOS (alta confianza)

### Criticos cerrados

- **BK-001 (baseline) — `ecosystem: 'Homebrew'` en OSV**: verificado con grep en `src/lib/security/osv-api.ts:125,143,184`. Las tres ocurrencias estan corregidas a `'Bitnami'`; ademas hay un comentario en linea 180 que documenta el motivo: "OSV.dev no soporta 'Homebrew' como ecosystem (HTTP 400)". **Cerrado con verificacion directa.**

### Criticos que SIGUEN ACTIVOS (no resueltos pese a aparicion baseline)

- **SEG-001 (baseline) — Tokens npm en .claude/settings.local.json**: ⚠️ **NO RESUELTO**. Verificacion directa post-auditoria (`grep -nE 'npm_[A-Za-z0-9]' .claude/settings.local.json`) confirma **4 tokens activos** en lineas 56, 58, 140, 242 de `.claude/settings.local.json`. Si bien `.claude/` esta en `.gitignore` (linea 11), los tokens estan en el disco local del equipo del desarrollador con permisos default. Cualquier acceso al equipo (acceso fisico, malware, sync de configuracion) los compromete. El security-auditor de Wave 3 marco "sin secretos en repo" pero su scope era codigo trackeado en git — los tokens no estan trackeados pero existen.
  *Severidad real:* Critica (igual que baseline).
  *Accion inmediata:*
    1. Revocar los 4 tokens en npmjs.com: `npm_GXoXjk...`, `npm_2Sb4Xv...`, `npm_3wJbDk...` (aparece 2 veces).
    2. Reemplazar `.claude/settings.local.json` por permisos basados en allowlist de comando sin embeber el token (usar `NPM_TOKEN` desde `~/.npmrc` o variable de entorno saneada).
    3. Rotar tokens en `~/.npmrc` y `~/.zshrc` si existen alli.

### Altos cerrados

- **GOV-001 — CI sin tests Swift**: ahora corre `xcodebuild test` en `macos-latest`; quality-auditor confirma 30 @Test methods en BrewBarTests.
- **GOV-005 — BrewBar no notarizado**: ahora hay `menubar/scripts/release.sh` con `xcrun notarytool`; pipeline documentado en CLAUDE.md.
- **UI-001 — Brewfile reconcile sin ConfirmDialog**: verificado en `brewfile.tsx` — ConfirmDialog presente.
- **UI-002 — Sync syncNow sin ConfirmDialog**: confirmado en sync.tsx — ConfirmDialog presente; baseline downgrade a Baja como UX-SYNC-01 (no menciona iCloud en mensaje).
- **UI-003 — Compliance remediate sin ConfirmDialog**: confirmado en compliance.tsx — ConfirmDialog presente.
- **QA-001 — Vistas TUI sin tests de renderizado**: el inventario nuevo reporta tests para `app.test.tsx`, `dashboard.test.tsx`, `outdated.test.tsx`, `services.test.tsx`, `installed.test.tsx`, `search.test.tsx`, `account.test.tsx`, `profiles.test.tsx`, `brewfile.test.tsx`, `sync.test.tsx`, `compliance.test.tsx`, `history.test.tsx`, `rollback.test.tsx`, `security-audit.test.tsx`, `package-info.test.tsx`, `doctor.test.tsx`. Cobertura sustancial.
- **UX-002 — Onboarding ausente**: ahora hay `WelcomeView` + `src/lib/onboarding.ts` + flag persistente.
- **BK-004 — `isExpired()` falla abierto**: nuevo reporte de backend confirma "fallo cerrado en fechas corruptas" (`isExpired()` trata `NaN` como expirado).
- **DS-001 — Tokens SPACING no adoptados**: frontend-auditor confirma "0 magic spacing numbers; todos los valores via SPACING tokens".
- **ACC-001 — NO_COLOR no implementado**: design-auditor confirma "NO_COLOR completo" como conforme.
- **UX-001 — Notificaciones CVE con identificadores fijos**: no aparece en reporte nuevo; presumiblemente resuelto pero conviene verificar identificadores en `SchedulerService.swift`.

### Medios/Bajos cerrados (muestra representativa)

- BK-008 (decryptPayload sin type guard): backend-auditor reporta validacion en runtime correcta en `validatePromoCode` y `redeemPromoCode`.
- ARQ-001 (schemas licencia divergentes): backend confirma `LastAction` y `LicenseData` Codable coherentes TS↔Swift.
- BK-007 (lock historial sin TTL): backend reporta "bloqueo de fichero en history-logger.ts via open(lockPath, 'wx') con TTL de 30s para stale locks".
- BK-002 (deadlock BrewProcess): backend confirma `BrewProcess` con `OnceGuard` y manejo correcto de timeouts.

---

## Hallazgos que PERSISTEN del baseline

### Crítico que sigue activo

- **SEG-002 (baseline) — ENCRYPTION_SECRET y SCRYPT_SALT hardcoded en bundle** → ahora reformulado: la nueva auditoria lo reconoce como diseno explicito documentado en CLAUDE.md ("AES key bundle-derivable; SEG-009 removed hardcoded built-in accounts because of this"). Hay un TODO SEG-003 pendiente sobre la clave legacy scrypt. **Ya no es bloqueante** porque el riesgo se mitigo de otra forma (eliminacion de built-in accounts hardcoded, machine binding + revalidacion server side). Pero la clave legacy scrypt en `license-manager.ts` y `LicenseChecker.swift:159-163` sigue activa con TODO sin resolver — ARC-05 en arquitectura nueva.

### Altos que siguen activos

- **SEG-003 (baseline) — Clave AES hex hardcoded en LicenseChecker.swift**: ARC-05 nuevo reporte (Media) — hex `5c3b2ae2…` sigue presente con TODO de >6 versiones.
- **ARQ-002 (baseline) — Divergencia politica degradacion 7d TS vs 30d Swift**: backend-auditor nuevo confirma "Degradacion en cuatro niveles: none / warning / limited / expired (alineado con TS y Swift)" — aparentemente alineado. Verificar reales valores antes de cerrar.
- **BK-003 (baseline) — Snapshots sin poda**: no se menciona explicitamente. Verificar.

---

## Hallazgos NUEVOS (no estaban en baseline)

### Critico nuevo

- **SEG-001 nuevo (=ARC-01/EP-7.1) — Flag injection en compliance-remediator**: `compliance-remediator.ts:18,29` pasa `v.packageName` a `streamBrew` sin `validatePackageName()`. Modulo nuevo introducido tras la 0.6.1 con la feature compliance. **No estaba en baseline porque la feature no existia.**

### Altos nuevos

- **SEG-002 nuevo — Flag injection en brewfile-manager**: `brewfile-manager.ts applyDrift()` mismo patron — feature nueva post-baseline.
- **SEG-003 nuevo — PII en Unified Log**: `LicenseChecker.swift:205,212` con `privacy: .public` imprime email y license key. Posible regresion o codigo nuevo no auditado en baseline.
- **QA-001 nuevo — Test `ConfirmDialog` locale `es` falla por timeout**: `confirm-dialog.test.tsx:44` rompe `npm run validate` HOY y bloquea pre-push. Issue activo.
- **UX-ACC-01 + UX-ACC-02 — Account no soporta tier Team**: tier Team aparentemente introducido entre baseline y hoy; UI no completa.
- **FE-3 — IPC handoff incompleto**: `search.tsx` install y `installed.tsx` uninstall no llaman `writeLastAction()`. Probablemente regresion post-introduccion de IPC (commit pipe handoff).
- **EP-9.1 — `SyncMonitor.getKnownMachineCount()` siempre 0**: bug confirmado de campo en JSON envelope. Probablemente nuevo (feature sync nueva o evolucionada post-baseline).
- **CODEOWNERS apunta a @MoLinesGitHub**: rotura de reviews automaticos por handle de org renombrada.

### Medios/Bajos nuevos

- ARC-02 (`BrewBarDesignVariants.swift` 991 LOC en binario produccion).
- ARC-04 (`async-state.ts` dead module).
- PERF-MED-01 (3 vistas sin selector granular).
- DA-MED-01 (`SettingsView` frame fijo corta con Dynamic Type AX1+).
- 198 archivos `.playwright-mcp/` trackeados pese a `.gitignore`.
- `homebrew/Formula/brew-tui.rb` y `homebrew/Casks/brewbar.rb` en repo desactualizados a 0.7.0 (9 releases atras).
- `npm audit` reporta dos vulnerabilidades moderadas (brace-expansion ReDoS, ws memory disclosure).
- Promo API sin retry (BK-EP-4.1).
- Polar 429 no reintentado (EP-1.2).
- Rate limit en memoria (EP-1.4).
- Directorio iCloud sin modo 0o700.
- `merge-union` declarado pero no implementado.

---

## Veredicto comparado

| Eje | Baseline 2026-05-01 | Actual 2026-05-21 |
|-----|--------------------|--------------------|
| Estado release | **NO apto sin correcciones** | **NO apto sin correcciones** |
| Razones bloqueantes | 6 (tokens npm, OSV roto, Cask URL 404, no notarizacion, formula obsoleta, isExpired falla abierto) | 4 (flag injection compliance + brewfile, PII en log, test ConfirmDialog rompe pre-push) |
| Bloqueantes resueltos vs nuevos | — | Resueltos 4 (no 5: SEG-001 sigue vivo), nuevos 3 → mejora marginal |
| Tokens npm activos en disco | 2 | **4** (verificado por grep) | **regresion** |

**Tendencia: el proyecto se ha estabilizado significativamente entre 0.6.1 y 1.2.1.** Los bloqueantes clasicos (tokens, OSV, notarizacion, Cask URL, isExpired) estan cerrados. Los nuevos bloqueantes son patrones que se reintrodujeron al ampliar features (compliance, brewfile, sync, tier Team) sin que la validacion `PKG_PATTERN` ya existente en `brew-api.ts` se replicara consistentemente.

---

## Recomendacion para release 1.3

0. **PRIMERO — Revocar 4 tokens npm activos** (esfuerzo XS, urgencia maxima):
   - Tokens en `.claude/settings.local.json:56,58,140,242` siguen explotables localmente.
   - Revocar en npmjs.com inmediatamente; rotar y cambiar a flujo sin embeber.

1. **Cerrar los 4 bloqueantes** (esfuerzo XS-S agregado < 1 dia):
   - Añadir `validatePackageName()` en `compliance-remediator.ts` y `brewfile-manager.ts`.
   - Cambiar `privacy: .public` a `.private` para email/license en `LicenseChecker.swift:205,212`.
   - Arreglar test `confirm-dialog.test.tsx` locale `es` (Ink race en setState — usar `act()` o esperar frame).
2. **Higiene de repo** (esfuerzo XS-S):
   - `git rm -r --cached .playwright-mcp/`.
   - Sync `homebrew/Formula/brew-tui.rb` y `homebrew/Casks/brewbar.rb` a 1.2.1 (o eliminarlos del repo si el canonico es el tap externo).
   - Update CODEOWNERS a `@MoLinesDesigns`.
3. **Hardenings residuales** post-release (esfuerzo S-M):
   - Account tier Team labels + deactivate.
   - `writeLastAction` en search.tsx + installed.tsx.
   - Resolver TODO scrypt legacy (SEG-003 baseline).
   - `npm audit fix` para brace-expansion + ws.

Una vez cerrados los 4 bloqueantes y la higiene de repo, el producto pasa de "NO apto" a "GO con caveats" para 1.3.
