# 21. Registro central de hallazgos

> Auditor: report-consolidator | Fecha: 2026-05-21 | Versión auditada: 1.2.1

## Resumen

* **Total hallazgos (deduplicados):** 61
* **Críticos:** 1
* **Altos:** 14
* **Medios:** 22
* **Bajos:** 24
* **Reportes analizados:** 14 de 14
* **Reportes faltantes:** Ninguno

---

## Nota de deduplicación

Los reportes individuales detectaron varios hallazgos redundantes. Los siguientes se unificaron bajo un único ID canónico con referencias cruzadas a las secciones origen:

| ID canónico | Secciones que lo detectaron |
|-------------|----------------------------|
| SEG-001 | ARC-01 (03-architecture) · EP-7.1 (13-endpoints) · hallazgo Alta de 08-security · UX-CMP-01 (12-screens) |
| SEG-002 | EP-7.2 (13-endpoints) · hallazgo Alta de 07-backend-persistence (11.3) |
| UI-001 | FE-1 (04-frontend 5.3) · UX-ACC-01 (06-UX) · 12-screens UX-ACC-01 |
| UI-002 | FE-2 (04-frontend 5.3) · UX-ACC-02 (06-UX) · 12-screens UX-ACC-02 |
| BK-001 | FE-3 (04-frontend 10.2) · SCR-INST-01 (12-screens) · EP-8.1 (13-endpoints) |
| GOV-001 | GOV (02-governance 2.5) · REL-18.3 (11-release) |
| GOV-002 | INV-1.7 (01-inventario) · GOV-2.4 (02-governance) |
| GOV-003 | GOV-2.5 homebrew/Formula (02-governance) · REL-18.3 (11-release) |
| GOV-004 | GOV-2.5 macports (02-governance) · REL-18.3 (11-release) |
| ARQ-001 | ARC-05 (03-architecture) · SEG-003 (08-security) · BK resiliencia (07-backend) |
| ARQ-002 | ARC-10 (03-architecture) · PERF-LOW-03 (10-performance) |
| ARQ-003 | ARC-07 (03-architecture) · PERF 10.8 (10-performance) |
| DS-001 | 7.1 motion tokens (06-design) · 8.3 reduce motion TUI (06-design) |
| ACC-001 | 8.3 reduce motion BrewBar (06-design) |
| DS-002 | DA-MED-01 (06-design 8.3) · 12-screens SettingsView (12-screens) |
| ARQ-004 | ARC-06 (03-architecture) · UX-PROF-01 relevante (05-ux) |
| PERF-001 | PERF-MED-01 (10-performance) · 12-screens services/doctor/outdated |

---

## Registro completo

### CRÍTICOS

| ID | Dominio | Subzona | Hallazgo | Severidad | Impacto | Evidencia | Acción | Estado |
|----|---------|---------|----------|-----------|---------|-----------|--------|--------|
| SEG-001 | Seguridad | Inyección de argumentos brew — compliance | `compliance-remediator.ts` pasa `v.packageName` directamente a `streamBrew(['install'/'upgrade', ...])` sin pasar por `validatePackageName()`. Un PolicyFile JSON artesanal puede especificar `--HEAD`, taps privados o paquetes con side-effects. El usuario introduce la ruta del PolicyFile vía TextInput. | Crítica | Instalación/upgrade de paquetes arbitrarios en el entorno Homebrew del usuario sin su consentimiento explícito | `src/lib/compliance/compliance-remediator.ts:18,29` · `src/lib/compliance/policy-io.ts` | 1) Llamar `validatePackageName(v.packageName)` de `brew-api.ts` antes de cada `streamBrew`. 2) Añadir validación de packageName en `isValidPolicy()` para rechazar el PolicyFile malformado en el punto de importación. | Pendiente |

---

### ALTOS

| ID | Dominio | Subzona | Hallazgo | Severidad | Impacto | Evidencia | Acción | Estado |
|----|---------|---------|----------|-----------|---------|-----------|--------|--------|
| SEG-002 | Seguridad | Inyección de argumentos brew — brewfile | `brewfile-manager.ts` `applyDrift()` llama `streamBrew(['install', name])` sin `validatePackageName()`. Un brewfile YAML manipulado puede inyectar flags como `--HEAD`. Patrón idéntico a SEG-001 en archivo distinto. | Alta | Instalación de paquetes con flags arbitrarios desde un Brewfile externo | `src/lib/brewfile/brewfile-manager.ts` en `applyDrift()` · EP-7.2 | Llamar `validatePackageName(name)` antes del spawn en `applyDrift()`, igual que hace `brew-api.ts` en todas sus rutas | Pendiente |
| SEG-003 | Seguridad | PII en Unified Log macOS | `LicenseChecker.swift` usa `privacy: .public` con `String(describing: status)` en dos puntos, filtrando `email`, `key`, `instanceId`, `customerName` al Unified Log del sistema (legible desde Console.app sin privilegios). | Alta | Exposición de email y clave de licencia del usuario en el log del sistema macOS, legible por otras apps con permisos de log | `menubar/BrewBar/Sources/Services/LicenseChecker.swift:205,212` | Implementar `CustomStringConvertible` en `LicenseStatus`/`LicenseData` que redacte campos sensibles, o cambiar la anotación a `privacy: .private` | Pendiente |
| UI-001 | UI | AccountView — tier Team sin etiqueta | El bloque de estados en `account.tsx:87-89` cubre `pro`/`free`/`expired` pero no `team`. Usuarios del tier Team ven el campo de estado vacío. | Alta | Usuarios Team no pueden confirmar visualmente su tier; el producto parece broken para ese segmento | `src/views/account.tsx:87-89` | Añadir rama `else if (status === 'team')` renderizando label `[Team]` con color correspondiente | Pendiente |
| UI-002 | UI | AccountView — deactivate bloqueado para Team | La condición `input === '2' && status === 'pro'` excluye el tier `team`. El footer muestra `2:deactivate` para usuarios Team, pero la acción no dispara. | Alta | Usuarios Team no pueden desactivar su licencia desde la UI; funcionalidad rota para ese tier | `src/views/account.tsx:36` | Cambiar condición a `(status === 'pro' \|\| status === 'team')` | Pendiente |
| BK-001 | Backend | IPC handoff BrewBar — install/uninstall | `search.tsx` no llama `writeLastAction()` tras install exitoso, e `installed.tsx` no lo llama tras uninstall. BrewBar no recibe la señal IPC y su banner de "última acción" nunca se refresca para esas operaciones. `remainingOutdated` tampoco se actualiza. | Alta | Canal IPC TUI↔BrewBar roto para instalaciones desde SearchView y desinstalaciones desde InstalledView; BrewBar muestra datos obsoletos | `src/views/search.tsx:81-84` (install) · `src/views/installed.tsx:212` (uninstall) · EP-8.1 | Añadir `writeLastAction({ action: 'install'/'uninstall', packages: [...], ... })` en el `.then()` del stream en ambas vistas | Pendiente |
| ARQ-005 | Arquitectura | DesignExploration en binario notariado | `DesignExploration/BrewBarDesignVariants.swift` (991 LOC) incluido en el target de producción via glob `Sources/**` en `Project.swift`. Archivo de exploración visual no productivo en el binario firmado y notariado. | Alta | Engrosamiento del binario y de su firma; dead code en producción; riesgo si el archivo contiene referencias no auditadas | `menubar/Project.swift:69` · `menubar/BrewBar/Sources/DesignExploration/BrewBarDesignVariants.swift` | Mover `DesignExploration/` fuera de `BrewBar/Sources/` o añadir exclusión explícita en `Project.swift` | Pendiente |
| QA-001 | Testing | Suite de tests roja — ConfirmDialog locale es | `confirm-dialog.test.tsx:44` — test `routes Spanish s/S to onConfirm only when locale is es` falla con `Test timed out in 5000ms`. Suite actual: `1 failed | 443 passed`. `npm run validate` fallaría en cualquier `git push` (pre-push hook). | Alta | Bloquea `git push` vía pre-push hook; hace inviable cualquier release a través del pipeline normal | `src/components/common/confirm-dialog.test.tsx:44` | Añadir `await new Promise(r => setImmediate(r))` entre `setState({ locale: 'es' })` y `stdin.write('s')` para garantizar que el render procesa el cambio de estado antes de emitir la tecla | Pendiente |
| GOV-001 | Gobierno | CODEOWNERS con handle legacy | `.github/CODEOWNERS:1` apunta a `* @MoLinesGitHub`. La organización fue renombrada a `MoLinesDesigns`. Reviews automáticos en PRs no funcionan. | Alta | Los pull requests no asignan reviewers automáticamente; las contribuciones externas quedan sin revisión formal | `.github/CODEOWNERS:1` | Actualizar a `* @MoLinesDesigns` o al usuario/equipo correcto en la org actual | Pendiente |
| GOV-002 | Gobierno | 198 archivos Playwright trackeados en git | `.playwright-mcp/` contiene 198 archivos (110 YAML, 60 PNG, 26 logs, 2 MD) trackeados en git a pesar de existir la regla `.gitignore:34`. Los archivos fueron commiteados antes de añadir la regla. | Alta | Historial git inflado con artefactos de sesiones internas; clones con artefactos de sesiones de trabajo privadas | `git ls-files .playwright-mcp/` (198 entradas) · `.gitignore:34` | Ejecutar `git rm -r --cached .playwright-mcp/` y hacer commit; la regla `.gitignore` ya existe | Pendiente |
| DS-001 | Design system | BlinkingText / Motion tokens sin reduce-motion | `BlinkingText` titila siempre a 600 ms sin consultar `NO_COLOR`, `REDUCE_MOTION` ni ninguna variable de entorno equivalente. No existe definición de motion tokens en el sistema. | Alta | Usuarios con epilepsia fotosensible o preferencia de reduce motion no tienen mecanismo de opt-out del parpadeo; afecta a todas las pantallas donde el header está visible | `src/components/common/blinking-text.tsx:23` · `src/utils/colors.ts` (sin motion tokens) | Definir constante de duración; detener el intervalo cuando `NO_COLOR` esté activo o exponer prop `disableAnimation`; documentar variable de entorno de opt-out | Pendiente |
| ACC-001 | Accesibilidad | Reduce Motion no verificado en BrewBar | Ninguna vista de BrewBar consulta `@Environment(\.accessibilityReduceMotion)`. Sin gate preventivo para animaciones futuras. | Alta | Si se añaden animaciones (probable en evolución de la app), no habrá control de reduce-motion; usuarios con configuración de accesibilidad no serán respetados | `menubar/BrewBar/Sources/Views/` — ningún archivo consulta `accessibilityReduceMotion` | Añadir comprobación preventiva en `PopoverView` y documentar el patrón de uso | Pendiente |
| GOV-003 | Gobierno | homebrew/Formula y Casks 9 versiones atrasados | `homebrew/Formula/brew-tui.rb` y `homebrew/Casks/brewbar.rb` en el repo declaran versión `0.7.0`; la publicada es `1.2.1`. El tap canónico real está correcto, pero estos archivos en el repo inducen a error a contribuidores. | Alta | Contribuidores leen versiones incorrectas; scripts que lean estos archivos obtienen datos 9 releases obsoletos | `homebrew/Formula/brew-tui.rb:4` · `homebrew/Casks/brewbar.rb:2` | Opción A (recomendada): eliminar `homebrew/Formula/` y `homebrew/Casks/` del repo — el source of truth es `MoLinesDesigns/homebrew-tap`. Opción B: actualizar a 1.2.1 con SHA256 actual. | Pendiente |
| BK-002 | Backend | SyncMonitor.getKnownMachineCount() siempre retorna 0 | `SyncMonitor.swift` hace `json["machines"]` sobre el JSON exterior del envelope, pero `machines` está dentro del payload AES-256-GCM cifrado. El campo es inaccesible sin descifrar. La UI de BrewBar muestra "0 dispositivos" permanentemente. | Alta | Dato confirmadamente incorrecto mostrado al usuario; feature de multi-dispositivo no funciona en BrewBar | `menubar/BrewBar/Sources/Services/SyncMonitor.swift` · EP-9.1 | Eliminar o no exponer `getKnownMachineCount()` desde Swift sin descifrar; o exponer `machineCount` en el plaintext del envelope al escribirlo desde TS | Pendiente |
| REL-001 | Release | release.sh sin verificación de perfil notary | `release.sh` no ejecuta `xcrun notarytool history` para validar las credenciales del perfil notary antes de iniciar el archive. Un perfil expirado o inválido se descubre tras ~10 minutos de build. | Alta | Pipeline de release se interrumpe tardíamente; waste significativo de tiempo de build | `menubar/scripts/release.sh:29-35` | Añadir preflight: `xcrun notarytool history --keychain-profile "$NOTARY_PROFILE" --limit 1 > /dev/null \|\| exit 1` al inicio del script | Pendiente |

---

### MEDIOS

| ID | Dominio | Subzona | Hallazgo | Severidad | Impacto | Evidencia | Acción | Estado |
|----|---------|---------|----------|-----------|---------|-----------|--------|--------|
| ARQ-001 | Arquitectura | Clave scrypt legacy activa (SEG-003) | Clave `legacyEncryptionKey` via `scryptSync` activa como fallback en `license-manager.ts` y `sync/crypto.ts`. TODO marcado para v0.6.3; proyecto en v1.2.1. En BrewBar: hex hardcodeado en `LicenseChecker.swift:159-163`. Extiende superficie de ataque offline. | Media | Superficie de clave fija conocida en bundle publicado; 9 versiones de mora sobre el plan de eliminación | `src/lib/license/license-manager.ts:103-106` · `src/lib/sync/crypto.ts:31` · `menubar/BrewBar/Sources/Services/LicenseChecker.swift:159-163` | Verificar telemetría de zero fallback decrypts; eliminar rama legacy en `license-manager`, `sync/crypto` y `LicenseChecker.swift`; el auto-upgrade a HKDF ya existe en ambos módulos | Pendiente |
| ARQ-002 | Arquitectura | impactCache sin TTL temporal | `analyzeDependencyImpact` usa `Map` con LRU-lite (max 64 entradas) pero sin TTL basado en tiempo. Datos pueden quedar stale si el proceso vive mucho tiempo o el usuario cambia versiones pinadas fuera de la app. | Media | Análisis de impacto potencialmente obsoleto mostrado al usuario sin indicación de antigüedad | `src/lib/brew-api.ts:197-227` | Añadir `cachedAt: Date.now()` por entrada y descartar si > 5 minutos; invalidar en `fetchInstalled` | Pendiente |
| ARQ-006 | Arquitectura | `async-state.ts` módulo muerto | `src/lib/async-state.ts` no tiene importadores en código de producción. Único consumidor: su propio test. | Media | Dead code que confunde a contribuidores y aumenta la superficie de mantenimiento | `src/lib/async-state.ts` · `src/lib/async-state.test.ts` | Eliminar ambos archivos | Pendiente |
| ARQ-007 | Arquitectura | Cross-store coupling en stores Pro | `cleanup-store`, `history-store`, `security-store`, `profile-store` importan `useLicenseStore` y/o `useBrewStore` via `.getState()`. Dificulta testing y sustitución de stores. | Media | Acoplamiento store↔store dificulta tests unitarios de stores Pro y aumenta el riesgo de regresiones silenciosas | `src/stores/cleanup-store.ts` et al. | Pasar `isPro` e `installedFormulae` como argumentos a las funciones de los stores Pro, igual que hacen los módulos `lib/` | Pendiente |
| ARQ-004 | Arquitectura | PKG_PATTERN duplicado con semántica divergente | `brew-api.ts`: `PKG_PATTERN = /^[\w@./+-]+$/` (permisivo, permite `/` y mayúsculas). `profile-manager.ts`: `PKG_PATTERN = /^[a-z0-9][-a-z0-9_.@+]*$/` (restrictivo, sin mayúsculas ni `/`). Los perfiles pueden rechazar nombres que brew-api acepta. | Media | Comportamiento inconsistente: paquetes con nombres válidos para Homebrew pueden ser rechazados en la gestión de perfiles | `src/lib/brew-api.ts` · `src/lib/profiles/profile-manager.ts` | Centralizar `PKG_PATTERN` en `brew-api.ts` y reexportarlo; decidir explícitamente qué patrón aplica en cada contexto | Pendiente |
| SEG-004 | Seguridad | `brew` resuelto via PATH heredado | `brew-cli.ts` invoca `spawn('brew', args, ...)` via PATH del entorno del usuario. Un PATH hijack desde un dotfile o entorno comprometido puede suplantar `brew`. | Media | Ejecución de un binario `brew` suplantado si el PATH del usuario está comprometido | `src/lib/brew-cli.ts:8,40` · EP-7.3 | Buscar brew en rutas canónicas (`/opt/homebrew/bin/brew`, `/usr/local/bin/brew`) antes del spawn, con fallback a `which brew` solo si la ruta es absoluta y ejecutable; igual que hace BrewBar | Pendiente |
| SEG-005 | Seguridad | Vulnerabilidades npm moderadas | `npm audit` reporta 2 vulnerabilidades moderadas: `brace-expansion < 3.0.3` (ReDoS, CVSS 5.3) y `ws < 8.17.1` (memory disclosure, CVSS 5.3). Fix disponible. | Media | Vulnerabilidades de supply chain con CVSS 5.3; el impacto práctico depende de si los paths vulnerables son alcanzables en la TUI | `npm audit` (2 vulnerabilidades moderadas con fix) | Ejecutar `npm audit fix`; verificar compatibilidad de semver; añadir `npm audit --audit-level=moderate` al gate de CI | Pendiente |
| SEG-006 | Seguridad | Rate limit volátil entre reinicios | Tracker de rate limit en `license-manager.ts` es variable de módulo en memoria. Un `kill -9` o crash reinicia los contadores; un atacante local puede eludir el lockout de 15 min relanzando el proceso. | Media | Bypass del mecanismo anti-fuerza-bruta de activación de licencia | `src/lib/license/license-manager.ts` (objeto `rateLimitTracker`) | Persistir estado de lockout en `~/.brew-tui/rate-limit.json` con modo `0o600` y TTL/fecha de expiración; leer al iniciar el módulo | Pendiente |
| BK-003 | Backend | Promo API sin retry en 5xx transiente | `promo.ts` usa `fetchWithTimeout`, no `fetchWithRetry`. Un fallo puntual de `api.molinesdesigns.com` aborta el canje de código promocional sin reintento. | Media | Usuario pierde la posibilidad de canjear un código promo en caso de error de red transitorio | `src/lib/license/promo.ts` | Reemplazar `fetchWithTimeout` por `fetchWithRetry` con mismo `retryOn` que Polar | Pendiente |
| BK-004 | Backend | Polar 429 no reintentado con backoff | `fetchWithRetry` por defecto solo reintenta `status >= 500`. Un 429 de Polar con `Retry-After` pasa como error definitivo al usuario. | Media | Activaciones/validaciones de licencia bloqueadas en picos de uso de Polar sin reintento | `src/lib/license/fetch-timeout.ts` (`retryOn` no incluye 429) · EP-1.2 | Añadir `status === 429` a `retryOn` con backoff `Retry-After`-aware | Pendiente |
| BK-005 | Backend | Path traversal en policy-io.ts | `loadPolicy(filePath)` y `exportReport(report, outputPath)` aceptan rutas arbitrarias sin sanear. Un path con `..` accede fuera de `~/.brew-tui/`. | Media | Lectura o escritura de archivos fuera del directorio de datos de la app | `src/lib/compliance/policy-io.ts` — `loadPolicy()` y `exportReport()` | Restringir rutas de entrada/salida a un directorio permitido o validar que no contienen `..` con `path.resolve` + guard | Pendiente |
| BK-006 | Backend | merge-union declarado pero no implementado | `ConflictResolution` en `src/lib/sync/types.ts` exporta `'merge-union'` pero `applyConflictResolutions()` en `sync-engine.ts` no tiene rama para este valor. Si un caller lo usa, la resolución se descarta silenciosamente. | Media | Silencio incorrecto: conflictos enviados con `merge-union` se ignoran sin error ni feedback | `src/lib/sync/types.ts` · `src/lib/sync/sync-engine.ts:applyConflictResolutions()` | Eliminar `'merge-union'` del tipo hasta implementarlo, o añadir la lógica; documentar la omisión con `// not yet implemented` | Pendiente |
| BK-007 | Backend | Directorio iCloud sin modo restrictivo | `mkdir(ICLOUD_SYNC_DIR, { recursive: true })` en `icloud-backend.ts` sin `mode: 0o700`. El directorio hereda permisos del sistema. | Media | Permisos más permisivos de lo necesario en el directorio de sincronización | `src/lib/sync/backends/icloud-backend.ts` · EP-9.4 | Añadir `mode: 0o700` al `mkdir` de `ICLOUD_SYNC_DIR` | Pendiente |
| DS-002 | Design system | SettingsView altura fija 540pt | `SettingsView.swift` tiene `.frame(width: 360, height: 540)`. Con Dynamic Type AX1+, la sección License puede quedar cortada y el contenido es inaccesible. | Media | Inaccesibilidad con Dynamic Type elevado; contenido de la vista Settings no visible para usuarios con necesidades de tamaño de fuente | `menubar/BrewBar/Sources/Views/SettingsView.swift:77` | Cambiar a `.frame(width: 360, minHeight: 500)` eliminando el máximo fijo, o usar `ScrollView` con `idealHeight` | Pendiente |
| UX-001 | UX | Rollback fase executing suprime Esc sin mensaje | Durante la fase `executing` de RollbackView, Esc está suprimido completamente sin ningún mensaje que informe al usuario que la operación no puede cancelarse. Parece colgado. | Media | Confusión crítica en una operación destructiva: el usuario no sabe si la app está respondiendo o ha fallado | `src/views/rollback.tsx` — fase `executing` | Mostrar mensaje inline fijo: "Operación en curso — no se puede cancelar" mientras `phase === 'executing'`; o permitir cancelación explícita | Pendiente |
| PERF-001 | Performance | Store entero en 3 vistas sin selector | `services.tsx`, `doctor.tsx` y `outdated.tsx` desestructuran `useBrewStore()` sin selector. Cualquier cambio en cualquier campo del store — incluyendo fetch de paquetes no relacionados — re-renderiza estas tres vistas completas. | Media | Re-renders innecesarios en vistas frecuentemente activas; puede causar parpadeos en terminales lentos con muchos paquetes | `src/views/services.tsx:34` · `src/views/doctor.tsx:15` · `src/views/outdated.tsx:65` | Migrar a selectors granulares como ya hace `dashboard.tsx:88-96` | Pendiente |
| QA-002 | Testing | data-dir.ts sin test | `writeLastAction()` (IPC crítico con BrewBar) y `ensureDataDirs()` no tienen test. Archivo de 74 líneas que gestiona el canal IPC y el directorio de datos. | Media | Fallo silencioso en `writeLastAction` puede romper el handoff IPC sin que los tests lo detecten | `src/lib/data-dir.ts` (74 LOC, sin test) | Añadir test unitario que mockee `node:fs/promises` y valide atomicidad (tmp+rename) de `writeLastAction` | Pendiente |
| QA-003 | Testing | 8 stores Pro sin tests unitarios | `brewfile-store`, `cleanup-store`, `compliance-store`, `history-store`, `profile-store`, `rollback-store`, `security-store`, `sync-store` — ninguno tiene archivo `.test.ts`. Solo testeados indirectamente a través de mocks de vistas. | Media | Transiciones de estado `loading`/`error` en stores Pro sin cobertura directa | `src/stores/` — 8 archivos sin `.test.ts` correspondiente | Añadir tests unitarios que verifiquen transiciones `loading`/`error` y que las acciones deleguen a la lib correspondiente | Pendiente |
| QA-004 | Testing | iCloud backend sin test | `icloud-backend.ts` (`isICloudAvailable`, `readSyncEnvelope`, `writeSyncEnvelope`) carece de test. El sync-engine está bien testeado pero el backend real no. | Media | Fallo silencioso si el path de iCloud cambia en una futura versión de macOS | `src/lib/sync/backends/icloud-backend.ts` | Añadir test de integración con `tmp` directory que valide las rutas de iCloud en el sistema | Pendiente |
| QA-005 | Testing | Analytics con 0 call sites en producción | `analytics.ts` define 10 eventos semánticos, taxonomía, consent y `registerSink()`. `grep -rn "from.*analytics\|import.*analytics" src/` (excluyendo `analytics.ts`) devuelve 0 líneas. Ningún KPI del producto es medible. | Media | Imposible medir funnels de activación, retención ni uso de features Pro; ceguera total sobre la salud del producto | `src/lib/analytics.ts` — módulo funcional sin llamadores de producción | Conectar al menos `activation_started/completed/failed` en `license-manager.ts` y `upgrade_prompt_shown` en `<UpgradePrompt>`; evaluar PostHog self-hosted o Plausible | Pendiente |
| GOV-004 | Gobierno | MacPorts descriptor obsoleto con checksums inválidos | `homebrew/macports/brew-tui.tcl:7` declara versión `0.1.0`; `rmd160` con zeros (inválido); homepage y maintainer apuntan a `@MoLinesGitHub` y URL legacy. | Media | Canal MacPorts no funcional; contribuidores o scripts que lo lean reciben datos erróneos | `homebrew/macports/brew-tui.tcl:7,12,20,22-24` | Si MacPorts no es un canal activo: eliminar o mover a `archive/`. Si es activo: actualizar versión, checksums, maintainer y homepage | Pendiente |
| REL-002 | Release | Crash reporting no activo en producción | El seam de crash reporting existe (opt-in) pero ningún endpoint está configurado en el binario distribuido. Crashes de Swift puro (`fatalError`, traps) no son capturables con la implementación actual. | Media | Imposible detectar regresiones de estabilidad en usuarios reales post-release | `src/lib/crash-reporter.ts` · `menubar/BrewBar/Sources/Services/CrashReporter.swift` (comentario QA-007) | Integrar Sentry-Cocoa para BrewBar + Sentry/Node para TUI como mínimo; o habilitar endpoint de crash reporting propio con opt-in en SettingsView | Pendiente |
| GOV-005 | Gobierno | Tuist no pinado en CI | `.github/workflows/ci.yml:38` descarga Tuist con `curl -Ls https://install.tuist.io \| bash` sin especificar versión. Un breaking release de Tuist puede romper el job silenciosamente. | Media | CI de BrewBar puede fallar silenciosamente tras un breaking release de Tuist | `.github/workflows/ci.yml:38` | Pinnar Tuist en CI mediante `mise` o `asdf`, o especificando versión en el comando curl | Pendiente |

---

### BAJOS

| ID | Dominio | Subzona | Hallazgo | Severidad | Impacto | Evidencia | Acción | Estado |
|----|---------|---------|----------|-----------|---------|-----------|--------|--------|
| ARQ-003 | Arquitectura | Data(contentsOf:) síncrono en @MainActor | `LastActionMonitor.swift:120` llama `Data(contentsOf: path)` en método `@MainActor`. El archivo es pequeño pero el I/O síncrono en main thread está desaconsejado. | Baja | I/O síncrono en main thread; riesgo bajo dado el tamaño del archivo | `menubar/BrewBar/Sources/Services/LastActionMonitor.swift:120` · ARC-07 | Mover la lectura a `Task { let data = try Data(contentsOf: path) }` | Pendiente |
| ARQ-008 | Arquitectura | Fire-and-forget Task sin handle en AppState | `Task { await refresh(force: true) }` en `AppState.swift:116` no almacena el handle. La tarea no es cancelable desde el exterior. | Baja | Tarea de refresh no cancelable; riesgo bajo por el guard `force || !isLoading` | `menubar/BrewBar/Sources/Models/AppState.swift:116` | Almacenar como `private var refreshTask: Task<Void, Never>?` y cancelar en `deinit` o `invalidate()` | Pendiente |
| ARQ-009 | Arquitectura | void doSearch en componente potencialmente desmontado | `src/views/search.tsx:179`: `onSubmit={() => void doSearch(query)}`. Si la vista se desmonta durante la búsqueda, `setResults` actualiza estado de componente desmontado. | Baja | Warning silencioso de React; sin impacto funcional visible | `src/views/search.tsx:179` | Considerar ref `isMounted` o `AbortController` para abortar la llamada en desmontaje | Pendiente |
| ARQ-010 | Arquitectura | outdated.tsx con >10 useState colocalizados | Vista con selección múltiple, lógica de upgrade por lotes, estado de confirmación y stream de progreso colocalizados. Más de 10 `useState` locales. | Baja | Mantenibilidad reducida; no impacto funcional | `src/views/outdated.tsx` | Extraer lógica de selección/actualización a custom hook `useOutdatedFlow` | Pendiente |
| ARQ-011 | Arquitectura | ConfirmDialog usa useInput directo sin documentar | `confirm-dialog.tsx:2,23` usa `useInput` de Ink directamente en lugar de `useViewInput`. Funcionalmente correcto vía `useModalStore`, pero viola la convención documentada y puede replicarse incorrectamente. | Baja | Riesgo de replicación incorrecta en nuevos componentes | `src/components/common/confirm-dialog.tsx:2,23` | Añadir comentario explicando que el uso de `useInput` directo es intencional y auto-guardado por `useModalStore` | Pendiente |
| UI-003 | UI | compliance.tsx y sync.tsx > 300 LOC con fases colocalizadas | `compliance.tsx` (348 LOC) y `sync.tsx` (347 LOC) tienen máquinas de estado internas (`phase` enum) colocalizadas en el body principal sin descomposición por fase. | Baja | Mantenibilidad reducida; dificultad para añadir nuevas fases sin efectos secundarios | `src/views/compliance.tsx` · `src/views/sync.tsx` | Extraer subcomponentes por fase en cada vista; el componente raíz coordina fases (~80 LOC) | Pendiente |
| UI-004 | UI | stat-card.tsx usa useTerminalSize en lugar de useContainerSize | `stat-card.tsx:10` usa `useTerminalSize().columns` (viewport completo) para calcular `minW`, en lugar de medir el contenedor real con `useContainerSize`. | Baja | Dimensionado incorrecto del componente si se reparenta en un panel más estrecho | `src/components/common/stat-card.tsx:10` | Migrar a `useContainerSize(ref)` para medir el contenedor real | Pendiente |
| UI-005 | UI | upgrade-prompt.tsx — width="80%" hardcodeado | `upgrade-prompt.tsx:46` — `width="80%"` no es un token del sistema de espaciado. | Baja | Incumplimiento menor del design system de spacing | `src/components/common/upgrade-prompt.tsx:46` | Cambiar a `flexGrow={1}` con `maxWidth` o eliminar el constraint | Pendiente |
| UI-006 | UI | pro-badge.tsx — dead code | `src/components/common/pro-badge.tsx` existe pero no tiene ningún importador en el resto del código; `header.tsx` inlinea el badge manualmente. | Baja | Dead code que confunde a contribuidores | `src/components/common/pro-badge.tsx` | Eliminar `pro-badge.tsx` o adoptarlo en `header.tsx` eliminando el inline | Pendiente |
| UI-007 | UI | history.tsx — retry ausente en estado de error | `history.tsx:114-115` — early return con `<ErrorMessage>` pero sin hint/botón de retry explícito. | Baja | UX inferior al estándar del producto (otras vistas muestran hint de retry) | `src/views/history.tsx:114-115` | Añadir hint de teclado para reintentar fetch (`r` o `1`) en el estado de error | Pendiente |
| UI-008 | UI | welcome.tsx — useEffect no-op | `welcome.tsx:18-20`: `useEffect(() => { return () => { /* cleanup not required */ }; }, [])` es dead code. | Baja | Dead code menor | `src/views/welcome.tsx:18-20` | Eliminar el `useEffect` vacío | Pendiente |
| UX-002 | UX | Literales en inglés en compliance.tsx | `compliance.tsx:80,91` — `(errors)` y `(warnings)` hardcodeados fuera de `t()`. Única cadena de UI en inglés en un producto con soporte i18n completo. | Baja | Inconsistencia de localización visible en la vista Compliance | `src/views/compliance.tsx:80,91` | Reemplazar por claves de traducción `compliance_errors_label` / `compliance_warnings_label` | Pendiente |
| UX-003 | UX | Perfil creado/editado sin feedback de éxito | Tras crear o editar un perfil, no hay `ResultBanner` ni mensaje de éxito. El usuario regresa al listado sin confirmación de que el guardado fue exitoso. | Baja | Confusión UX; el usuario puede repetir la acción creyendo que falló | `src/views/profiles/profile-create-flow.tsx` · `src/views/profiles/profile-edit-flow.tsx` | Mostrar `ResultBanner` de éxito tras crear/editar perfil, consistente con el resto del producto | Pendiente |
| UX-004 | UX | Sync: Enter silencioso con conflictos pendientes | `sync.tsx:246-250` — `if (pending.length === 0) setPhase('confirming-apply'); return;` sin feedback cuando quedan conflictos por resolver. | Baja | Usuario no sabe cuántos conflictos quedan para poder continuar | `src/views/sync.tsx:246-250` | Mostrar mensaje inline indicando cuántos conflictos quedan pendientes antes de continuar | Pendiente |
| UX-005 | UX | UpgradePrompt sin atajo a AccountView | `upgrade-prompt.tsx` muestra URL de compra pero no ofrece atajo de teclado para navegar directamente a AccountView donde está el flujo de activación. | Baja | Fricción innecesaria en el camino de conversión de Free a Pro | `src/components/common/upgrade-prompt.tsx` | Añadir hint `a:cuenta` para navegar directamente a AccountView | Pendiente |
| UX-006 | UX | BrewBar — .destructive role en "Upgrade All" | El `confirmationDialog` de "Upgrade All" en BrewBar usa role `.destructive` (botón rojo). Upgrade no es una acción destructiva; genera alarma innecesaria con el asistivo. | Baja | Señal visual incorrecta; confusión con acciones genuinamente destructivas | `menubar/BrewBar/Sources/Views/OutdatedListView.swift` | Cambiar role a `.default` o `.prominent` | Pendiente |
| DS-003 | Design system | Magic number en dashboard.tsx | `dashboard.tsx:161` — `const isNarrow = columns < 60`. No corresponde a ningún valor de `BREAKPOINTS` (`narrow=50`, `mid=80`). | Baja | Punto de ruptura no documentado que puede diferir del comportamiento esperado del design system | `src/views/dashboard.tsx:161` | Reemplazar por `getLayoutMode(columns)` o comparar contra `BREAKPOINTS.mid` | Pendiente |
| DS-004 | Design system | SectionHeader emoji sin fallback ASCII | `section-header.tsx`: prop `emoji` renderiza el carácter directamente sin alternativa para terminales sin UTF-8 extendido o conexiones SSH básicas. | Baja | Caracteres `?` o cajas vacías en terminales antiguas o SSH sin UTF-8 | `src/components/common/section-header.tsx` | Exponer prop `asciiIcon` como fallback; verificar `process.env.TERM` | Pendiente |
| ACC-002 | Accesibilidad | Sin .accessibilityAction alternativa en OutdatedListView | `OutdatedListView` en BrewBar expone la acción de upgrade solo como botón visible. No hay `.accessibilityAction` alternativa para usuarios que navegan solo con VoiceOver. | Baja | Usuarios con VoiceOver no pueden disparar upgrade individual sin tap | `menubar/BrewBar/Sources/Views/OutdatedListView.swift` | Añadir `.accessibilityAction(named: "Upgrade")` a la fila agrupada | Pendiente |
| ACC-003 | Accesibilidad | PrivacyInfo.xcprivacy — razones de filesystem no declaradas | BrewBar accede a timestamps de archivos en `~/.brew-tui/` y al directorio iCloud pero solo declara `NSPrivacyAccessedAPICategoryUserDefaults` en `PrivacyInfo.xcprivacy`. Apple puede requerir razones `C617.1` (file timestamp) y `E174.1` (disk space). | Baja | Posible rechazo en futuras revisiones de Apple Submission | `menubar/BrewBar/Resources/PrivacyInfo.xcprivacy` | Auditar llamadas `FileManager` en BrewBar; añadir razones faltantes al array `NSPrivacyAccessedAPITypes` | Pendiente |
| BK-008 | Backend | Trailing slash ausente en endpoints Polar | `polar-api.ts:63` — `${BASE_URL}/${endpoint}` produce `/activate` sin trailing slash. Polar especifica trailing slash; sin ella, `curl -L` haría 307 y perdería `Authorization`. Sin evidencia de fallo en producción. | Baja | Posible fallo silencioso si Polar cambia comportamiento del redirect | `src/lib/license/polar-api.ts:63` · EP-1.1 | Añadir trailing slash al template o a cada literal de endpoint | Pendiente |
| BK-009 | Backend | Machine ID enviado sin hashear a Polar | `polar-api.ts:95` — `label: machineId` envía el UUID en claro. Es un identificador opaco pero correlacionable en logs de Polar. | Baja | Posible correlación de identidad del usuario en logs de terceros | `src/lib/license/polar-api.ts:95` · EP-1.3 | Hash truncado SHA-256 antes de enviar como label (opcional; privacidad preventiva) | Pendiente |
| QA-006 | Testing | Suite BrewBar documentada incorrectamente en CLAUDE.md | `CLAUDE.md` dice "BrewBar: no tests written yet". Realidad: 30 métodos `@Test` en `BrewBarTests.swift` y `ServiceTests.swift` usando Swift Testing. | Baja | Documentación desactualizada que confunde a contribuidores | `CLAUDE.md` línea sobre BrewBar tests | Actualizar CLAUDE.md: "BrewBarTests: 30 tests con Swift Testing cubriendo modelos, LicenseChecker y AppState" | Pendiente |
| REL-003 | Release | release.sh no valida MARKETING_VERSION post-archive | `release.sh` no verifica que `CFBundleShortVersionString` del `.app` archivado coincida con `package.json`. | Baja | Version drift silenciosa en el binario distribuido | `menubar/scripts/release.sh` | Añadir verificación post-archive: `plutil -extract CFBundleShortVersionString raw` + comparar con `jq -r .version package.json` | Pendiente |
| GOV-006 | Gobierno | exportOptions.plist no en repo | `exportOptions.plist` necesario para `release.sh` está gitignoreado. Un clone limpio no puede ejecutar el release sin recrearlo manualmente. | Baja | Onboarding de nuevos desarrolladores en el pipeline de release requiere paso manual no documentado | Local en disco; gitignoreado | Proporcionar plantilla `exportOptions.plist.example` en el repo o documentar la estructura en `release.sh` | Pendiente |
| GOV-007 | Gobierno | Umbrales de cobertura inactivos en CI | `vitest.config.ts:24-31` declara umbrales de cobertura (50/60%) pero `npm run validate` no pasa `--coverage`; el gate nunca dispara. | Baja | Degradación silenciosa de cobertura sin alertas en CI | `vitest.config.ts:24-31` · `.github/workflows/ci.yml` | Añadir job separado en CI con `--coverage` o eliminar los umbrales si no hay plan de activarlos | Pendiente |
